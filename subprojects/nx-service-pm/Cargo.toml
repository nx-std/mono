[package]
name = "nx-service-pm"
version = "0.1.0"
edition = "2024"

[lib]
name = "nx_service_pm"
crate-type = ["rlib"]
test = false
doctest = false
bench = false

[features]
# Exposes session accessors on each `Pm*Service` for FFI consumers.
ffi = []

[dependencies]
nx-panic-handler = { version = "0.1.0", path = "../nx-panic-handler" }
nx-service-sm = { version = "0.1.0", path = "../nx-service-sm" }
nx-sf = { version = "0.1.0", path = "../nx-sf" }
nx-sys-thread-tls = { version = "0.1.0", path = "../nx-sys-thread-tls" }
bitflags = "2"
static_assertions = "1"
thiserror = { version = "2", default-features = false }
