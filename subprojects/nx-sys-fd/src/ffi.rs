//! FFI bindings for the `nx-sys-fd` crate.
//!
//! Grouped by override target: every symbol this crate replaces comes from `libsysbase`, so the
//! whole surface and the C types it is expressed in live in one subtree.

mod libsysbase;
